<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_expired_medicines_report_wizard_form" model="ir.ui.view">
        <field name="name">pharmacy.expired.medicines.report.wizard.form</field>
        <field name="model">pharmacy.expired.medicines.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Expired Medicines Report">
                <group>
                    <group>
                        <field name="month"/>
                        <field name="year" options="{'format': False}"/>
                        <field name="branch_ids"
                               widget="many2many_tags"
                               options="{'no_create': True, 'no_open': True}"
                               placeholder="Select Expired location(s) only"/>
                    </group>
                    <group>
                        <field name="date_from" readonly="1"/>
                        <field name="date_to" readonly="1"/>
                    </group>
                </group>
                <footer>
                    <button name="action_print_pdf" type="object" string="Print PDF" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_expired_medicines_report_wizard" model="ir.actions.act_window">
        <field name="name">Expired Medicines Report</field>
        <field name="res_model">pharmacy.expired.medicines.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
